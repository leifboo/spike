#!/usr/bin/env spike

extern cdecl int puts() {}

main(argv) {
    obj i;
    
    puts(2 < 3 ? "correct" : null);
    if (2 < 3)
        puts("correct");
    i = 1;
    while (i < 3)
        i = i + 1;
    if (i >= 3)
        puts("correct");
}
