#!/usr/bin/env spike

class Program {
    main() {
        var result;
        result = self.fib(10);
        result.print();
        return 0;
    }
    fib(x) {
        if (x<=2) return 1;
        return self.fib(x-1) + self.fib(x-2);
    }
}

