#!/usr/bin/env spike-1

// see http://paulgraham.com/accgen.html

// XXX
xxx(n) {
    acc(i) { return n += i; }
    return acc;
}

foo(n) {
    return [arg i; n += i];
}

main() {
    var acc, sum;
    
    acc = foo(10);
    sum = acc(10);
    sum = acc(20);
    sum = acc(2);
    stdout.printf("%d\n", sum);
    return 0;
}
